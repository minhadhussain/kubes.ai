import { ModulePlaceholder } from "@/components/module-placeholder";

export default function AnalyticsPage() {
  return (
    <ModulePlaceholder
      title="Operational analytics"
      description="Analytics will answer practical questions on lead conversion, active listings, transaction velocity, and expected revenue using production data only."
      priority="P0"
    />
  );
}
