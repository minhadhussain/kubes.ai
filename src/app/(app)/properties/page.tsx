import { ModulePlaceholder } from "@/components/module-placeholder";

export default function PropertiesPage() {
  return (
    <ModulePlaceholder
      title="Property database"
      description="Properties remain distinct from listings so buyer tours, notes, media, offers, and transactions can reference the same canonical record."
      priority="P0"
    />
  );
}
