import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ListingsPage() {
  return (
    <ModulePlaceholder
      title="Listing lifecycle"
      description="Listing creation, market status, showing instructions, and seller-side activity will live here without embedding future MLS assumptions into the core property model."
      priority="P0"
    />
  );
}
