import { ModulePlaceholder } from "@/components/module-placeholder";

export default function FinancePage() {
  return (
    <ModulePlaceholder
      title="Commission and finance"
      description="Commissions, expenses, and deal-level revenue summaries will roll up from transaction data instead of separate spreadsheets or manual calculators."
      priority="P0"
    />
  );
}
