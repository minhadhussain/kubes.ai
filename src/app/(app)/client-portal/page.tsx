import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ClientPortalPage() {
  return (
    <ModulePlaceholder
      title="Client portal foundation"
      description="Buyer and seller portal experiences are planned after the core internal workflow is reliable, with organization-safe access separated from the agent workspace."
      priority="P1"
    />
  );
}
