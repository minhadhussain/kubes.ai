import { ModulePlaceholder } from "@/components/module-placeholder";

export default function CrmPage() {
  return (
    <ModulePlaceholder
      title="CRM foundation"
      description="Next in sequence: central contacts, notes, tags, and activity timeline that feed leads, showings, offers, and transactions without duplicate entry."
      priority="P0"
    />
  );
}
